# M5.1 starting state

Base commit: `a9f626a0f7d4370f262341b7cb3476196d892112` (v0.1.0 release source).

Confirmed audit findings at M5.1 start:

- v0.1.0 `.skill.zip` uses unsupported private `internal-skills/` multi-Skill layout.
- release `source_commit` is declaration-only and can be falsified by any 40-hex value.
- current artifact includes `validate_project_state.py` while excluding its `listing-hardening` dependency.
- Router/Golden Path tests are deterministic contract checks, not real-agent E2E evidence.
- GitHub reports `main` unprotected and no repository rulesets.

M0–M4 business workflow architecture is preserved.
