# M5.1 RED test intent

The new `scripts/selftest_release_reliability.py` is intentionally added before the M5.1 implementation. It must fail against the v0.1.0 release implementation because the repository does not yet expose an official Plugin artifact contract, `verify_git_source()`, or explicit validator rejection for the old `internal-skills/` layout and broken legacy shim.

This file is temporary TDD evidence and may be removed before the final M5.1 PR if the GitHub Actions RED run is recorded in the PR body.
